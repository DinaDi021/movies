import { CircularProgress } from "@mui/material";
import React, { FC } from "react";

const IsLoading: FC = () => {
  return (
    <div>
      <CircularProgress />
    </div>
  );
};

export { IsLoading };
